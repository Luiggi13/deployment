name: E2E Tests

on:
  workflow_run:
    workflows: ["Unit Tests"]
    types:
      - completed

jobs:
  e2e_tests:
    runs-on: ubuntu-latest
    needs: common_steps
    steps:
      - name: 🧪 E2E 🧪
        run: npm run test:e2e
